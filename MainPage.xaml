<Page
    x:Class="EmployeeApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EmployeeApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <GridView Name = "Employee" ItemsSource="{Binding}" Height="300" Width="400" Margin="50" ScrollViewer.VerticalScrollBarVisibility="Auto">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="local:Employees">
                    <StackPanel Orientation="Horizontal" Margin="2">
                        <TextBlock Text="{x:Bind Name}" Width="100" Margin="2"/>
                        <TextBlock Text="{x:Bind Title}" Width="100" Margin="2" x:Phase="1"/>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
        <Button Content="Add Employee" Margin="281,94,0,0" Click="Button_Click" HorizontalAlignment="Center" VerticalAlignment="Top" />
    </Grid>
</Page>
